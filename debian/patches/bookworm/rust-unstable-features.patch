error[E0658]: use of unstable library feature 'error_in_core'
   --> ../../third_party/rust/chromium_crates_io/vendor/ixdtf-v0_5/src/error.rs:109:6
    |
109 | impl core::error::Error for ParseError {}
    |      ^^^^^^^^^^^^^^^^^^
    |
    = note: see issue #103765 <https://github.com/rust-lang/rust/issues/103765> for more information
    = help: add `#![feature(error_in_core)]` to the crate attributes to enable


..and other missing feature errors

--- a/third_party/rust/chromium_crates_io/vendor/calendrical_calculations-v0_2/src/lib.rs
+++ b/third_party/rust/chromium_crates_io/vendor/calendrical_calculations-v0_2/src/lib.rs
@@ -7,6 +7,9 @@
 // the Apache License, Version 2.0 which can be found at the calendrical_calculations
 // package root or at http://www.apache.org/licenses/LICENSE-2.0.
 
+#![feature(inline_const)]
+#![feature(error_in_core)]
+
 //! Calendrical calculations
 //!
 //! This crate implements algorithms from
--- a/third_party/rust/chromium_crates_io/vendor/ixdtf-v0_5/src/lib.rs
+++ b/third_party/rust/chromium_crates_io/vendor/ixdtf-v0_5/src/lib.rs
@@ -2,6 +2,8 @@
 // called LICENSE at the top level of the ICU4X source tree
 // (online at: https://github.com/unicode-org/icu4x/blob/main/LICENSE ).
 
+#![feature(error_in_core)]
+
 //! Parsers for extended date time string and Duration parsing.
 //!
 //! The [Internet Extended Date/Time Fmt (IXDTF)][rfc9557] is laid out by RFC 9557. RFC 9557
--- a/third_party/rust/chromium_crates_io/vendor/zerotrie-v0_2/src/lib.rs
+++ b/third_party/rust/chromium_crates_io/vendor/zerotrie-v0_2/src/lib.rs
@@ -2,6 +2,9 @@
 // called LICENSE at the top level of the ICU4X source tree
 // (online at: https://github.com/unicode-org/icu4x/blob/main/LICENSE ).
 
+#![feature(split_at_checked)]
+#![feature(error_in_core)]
+
 //! A data structure offering zero-copy storage and retrieval of byte strings, with a focus
 //! on the efficient storage of ASCII strings. Strings are mapped to `usize` values.
 //!
--- a/third_party/rust/chromium_crates_io/vendor/zerovec-v0_11/src/lib.rs
+++ b/third_party/rust/chromium_crates_io/vendor/zerovec-v0_11/src/lib.rs
@@ -2,6 +2,11 @@
 // called LICENSE at the top level of the ICU4X source tree
 // (online at: https://github.com/unicode-org/icu4x/blob/main/LICENSE ).
 
+#![feature(error_in_core)]
+#![feature(slice_split_at_unchecked)] 
+#![feature(split_at_checked)]
+#![feature(inline_const)]
+
 //! Zero-copy vector abstractions for arbitrary types, backed by byte slices.
 //!
 //! `zerovec` enables a far wider range of types — beyond just `&[u8]` and `&str` — to participate in
--- a/third_party/rust/chromium_crates_io/vendor/tinystr-v0_8/src/lib.rs
+++ b/third_party/rust/chromium_crates_io/vendor/tinystr-v0_8/src/lib.rs
@@ -2,6 +2,8 @@
 // called LICENSE at the top level of the ICU4X source tree
 // (online at: https://github.com/unicode-org/icu4x/blob/main/LICENSE ).
 
+#![feature(error_in_core)]
+
 //! `tinystr` is a utility crate of the [`ICU4X`] project.
 //!
 //! It includes [`TinyAsciiStr`], a core API for representing small ASCII-only bounded length strings.
--- a/third_party/rust/chromium_crates_io/vendor/icu_collections-v2/src/lib.rs
+++ b/third_party/rust/chromium_crates_io/vendor/icu_collections-v2/src/lib.rs
@@ -2,6 +2,9 @@
 // called LICENSE at the top level of the ICU4X source tree
 // (online at: https://github.com/unicode-org/icu4x/blob/main/LICENSE ).
 
+#![feature(error_in_core)]
+#![feature(inline_const)]
+
 //! Efficient collections for Unicode data.
 //!
 //! This module is published as its own crate ([`icu_collections`](https://docs.rs/icu_collections/latest/icu_collections/))
--- a/third_party/rust/chromium_crates_io/vendor/icu_locale_core-v2/src/lib.rs
+++ b/third_party/rust/chromium_crates_io/vendor/icu_locale_core-v2/src/lib.rs
@@ -2,6 +2,11 @@
 // called LICENSE at the top level of the ICU4X source tree
 // (online at: https://github.com/unicode-org/icu4x/blob/main/LICENSE ).
 
+#![feature(split_at_checked)]
+#![feature(slice_split_at_unchecked)]
+#![feature(inline_const)]
+#![feature(error_in_core)]
+
 //! Parsing, manipulating, and serializing Unicode Language and Locale Identifiers.
 //!
 //! This module is published as its own crate ([`icu_locale_core`](https://docs.rs/icu_locale_core/latest/icu_locale_core/))
--- a/third_party/rust/chromium_crates_io/vendor/icu_provider-v2/src/lib.rs
+++ b/third_party/rust/chromium_crates_io/vendor/icu_provider-v2/src/lib.rs
@@ -2,6 +2,9 @@
 // called LICENSE at the top level of the ICU4X source tree
 // (online at: https://github.com/unicode-org/icu4x/blob/main/LICENSE ).
 
+#![feature(error_in_core)]
+#![feature(inline_const)]
+
 //! `icu_provider` is one of the `ICU4X` components.
 //!
 //! Unicode's experience with ICU4X's parent projects, ICU4C and ICU4J, led the team to realize
--- a/third_party/rust/chromium_crates_io/vendor/icu_locale-v2/src/fallback/mod.rs
+++ b/third_party/rust/chromium_crates_io/vendor/icu_locale-v2/src/fallback/mod.rs
@@ -2,6 +2,8 @@
 // called LICENSE at the top level of the ICU4X source tree
 // (online at: https://github.com/unicode-org/icu4x/blob/main/LICENSE ).
 
+#![feature(inline_const)]
+
 //! Tools for locale fallback, enabling arbitrary input locales to be mapped into the nearest
 //! locale with data.
 
--- a/third_party/rust/chromium_crates_io/vendor/icu_locale-v2/src/lib.rs
+++ b/third_party/rust/chromium_crates_io/vendor/icu_locale-v2/src/lib.rs
@@ -2,6 +2,8 @@
 // called LICENSE at the top level of the ICU4X source tree
 // (online at: https://github.com/unicode-org/icu4x/blob/main/LICENSE ).
 
+#![feature(inline_const)]
+
 //! Canonicalization of locale identifiers based on [`CLDR`] data.
 //!
 //! This module is published as its own crate ([`icu_locale`](https://docs.rs/icu_locale/latest/icu_locale/))
--- a/third_party/rust/chromium_crates_io/vendor/icu_calendar-v2/src/lib.rs
+++ b/third_party/rust/chromium_crates_io/vendor/icu_calendar-v2/src/lib.rs
@@ -2,6 +2,10 @@
 // called LICENSE at the top level of the ICU4X source tree
 // (online at: https://github.com/unicode-org/icu4x/blob/main/LICENSE ).
 
+#![feature(inline_const)]
+#![feature(const_fn_floating_point_arithmetic)]
+#![feature(error_in_core)]
+
 //! Types for dealing with dates and custom calendars.
 //!
 //! This module is published as its own crate ([`icu_calendar`](https://docs.rs/icu_calendar/latest/icu_calendar/))
--- a/third_party/rust/chromium_crates_io/vendor/temporal_rs-v0_0/src/lib.rs
+++ b/third_party/rust/chromium_crates_io/vendor/temporal_rs-v0_0/src/lib.rs
@@ -90,6 +90,7 @@
 //! [ixdtf]: https://www.rfc-editor.org/rfc/rfc9557.txt
 //! [proposal]: https://github.com/tc39/proposal-temporal
 //! [spec]: https://tc39.es/proposal-temporal/
+#![feature(inline_const)]
 #![doc(
     html_logo_url = "https://raw.githubusercontent.com/boa-dev/boa/main/assets/logo.svg",
     html_favicon_url = "https://raw.githubusercontent.com/boa-dev/boa/main/assets/logo.svg"
