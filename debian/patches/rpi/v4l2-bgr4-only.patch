--- a/media/gpu/v4l2/v4l2_utils.cc
+++ b/media/gpu/v4l2/v4l2_utils.cc
@@ -509,6 +509,9 @@ std::vector<uint32_t> EnumerateSupported
                       : "pixel format: ")
               << FourccToString(fmtdesc.pixelformat) << " ("
               << fmtdesc.description << ")";
+
+    if (fmtdesc.pixelformat == V4L2_PIX_FMT_BGR32)
+      return std::vector<v4l2_enum_type>(1, fmtdesc.pixelformat);
     pix_fmts.push_back(fmtdesc.pixelformat);
   }
 
