author: Andres Salomon <dilinger@debian.org>
description: drop various arguments that clang-13 doesn't support

These usually show up as either warnings or actual errors; eg,

warning: unknown warning option '-Wno-deprecated-non-prototype' [-Wunknown-warning-option]
error: unknown argument: '-no-opaque-pointers'


Index: chromium-104.0.5112.65/build/config/compiler/BUILD.gn
===================================================================
--- chromium-104.0.5112.65.orig/build/config/compiler/BUILD.gn
+++ chromium-104.0.5112.65/build/config/compiler/BUILD.gn
@@ -1537,7 +1537,6 @@ config("default_warnings") {
 
       if (!is_nacl) {
         # TODO(https://crbug.com/1300731) Clean up and enable.
-        cflags += [ "-Wno-unqualified-std-cast-call" ]
       }
 
       if (!is_nacl && !(is_chromeos ||
@@ -1545,7 +1544,6 @@ config("default_warnings") {
         # TODO(https://crbug.com/1322823): Remove flags once potential miscompile is investigated.
         cflags += [
           "-Xclang",
-          "-no-opaque-pointers",
         ]
       }
 
